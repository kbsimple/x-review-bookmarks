---
quick_id: 260718-s4z
slug: statusz-hash-route
date: 2026-07-18
---

# Quick Task 260718-s4z: #statusz hash route

Add `#statusz` hash route to the static Netlify viewer. Navigating to `#statusz`
renders a live status table with build-time metadata and JS runtime state.

## Tasks

### Task 1: CSS + version constant + renderStatusz() + hash routing
**Files:** `src/services/static_export.py`
- Add `.statusz-page` and `.statusz-table` CSS
- Add `const XBM_VERSION = '0.1.0'` to global state block
- Add `renderStatusz()` function after `showDeepLinkError()`
- Update early hash detection to apply `deep-link-mode` class for `#statusz`
- Add `if (hash === '#statusz') { renderStatusz(); return; }` in bootstrap

### Task 2: String-grep tests
**Files:** `tests/test_static_export_service.py`
- Add `TestIndexHtmlStatusz` with 4 tests
