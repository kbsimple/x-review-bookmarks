---
phase: 10-cli-display
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_cli_display.py
  - tests/conftest.py
autonomous: true
requirements:
  - CLI-06
  - CLI-07
  - CLI-08
user_setup: []
must_haves:
  truths:
    - "Tests define quote tweet rendering behavior"
    - "Tests define retweet rendering behavior"
    - "Tests define unavailable post handling"
    - "Tests define media URL display behavior"
  artifacts:
    - path: "tests/test_cli_display.py"
      provides: "Test coverage for all CLI display requirements"
      contains: "test_quote_tweet_rendering, test_retweet_rendering, test_unavailable_placeholder, test_embedded_media_urls"
    - path: "tests/conftest.py"
      provides: "Test fixtures for post types"
      contains: "quote_tweet_post, retweet_post, unavailable_post"
  key_links:
    - from: "tests/test_cli_display.py"
      to: "src/cli/display.py"
      via: "pytest fixtures and display_post calls"
      pattern: "display_post\\(console, post, embedded_post=...\\)"
---

<objective>
Create test scaffolding for CLI embedded post rendering, establishing the expected behavior for quote tweets, retweets, unavailable posts, and media URL display.

Purpose: Define test cases that document expected rendering behavior before implementation. Tests will fail (RED) and guide implementation in subsequent plans.
Output: Test file with fixtures and test cases for all CLI-06, CLI-07, CLI-08 requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-cli-display/10-CONTEXT.md
@.planning/phases/10-cli-display/10-RESEARCH.md

<!-- Existing code patterns -->
@src/cli/display.py
</context>

<interfaces>
<!-- Key types and contracts from existing code -->

From src/cli/display.py:
```python
def display_post(
    console: Console,
    post: dict[str, Any],
    topics: Optional[list[dict[str, Any]]] = None,
    extra_metadata: Optional[list[tuple[str, str]]] = None,
    show_note: bool = True,
) -> None:
    """Display a single post in full-panel format."""
```

From src/repositories/posts.py:
```python
def _row_to_dict_with_embedded(self, row: sqlite3.Row) -> dict[str, Any]:
    """Returns post dict with 'embedded_post' key for retweets/quotes."""
```

Embedded post structure (from posts._row_to_dict_with_embedded):
```python
post['embedded_post'] = {
    'x_post_id': str,
    'created_at': str,
    'text': str,
    'author_id': str,
    'author_username': str,
    'author_display_name': str,
    'media_urls': list[str],
    'link_urls': list[str],
    'available': bool,
}
```

Post types (from posts table):
- 'original' - standard bookmarked post
- 'retweet' - reposted without commentary
- 'quote' - reposted with commentary
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test fixtures for post types</name>
  <files>tests/conftest.py, tests/test_cli_display.py</files>
  <behavior>
    - Test 1: quote_tweet_post fixture returns dict with post_type='quote', text for user commentary, and embedded_post dict
    - Test 2: retweet_post fixture returns dict with post_type='retweet', and embedded_post dict with original content
    - Test 3: unavailable_post fixture returns dict with embedded_post where available=False
    - Test 4: original_post_with_media fixture returns dict with media_urls list
  </behavior>
  <read_first>
    - tests/conftest.py (existing fixtures pattern)
    - tests/test_cli_display.py (new file, doesn't exist)
  </read_first>
  <action>
Create test fixtures in tests/conftest.py for embedded post testing. Per D-01 through D-08, fixtures must include:

1. `quote_tweet_post` fixture:
   - post_type='quote'
   - author_username, author_display_name for quoter
   - text with user's commentary
   - embedded_post dict containing:
     - author_username, author_display_name for quoted author
     - text for quoted content
     - media_urls list (can be empty)
     - available=True

2. `retweet_post` fixture:
   - post_type='retweet'
   - author_username for retweeter
   - embedded_post dict containing:
     - author_username, author_display_name for original author
     - text for original content
     - media_urls list
     - available=True

3. `unavailable_post` fixture:
   - post_type='retweet' or 'quote'
   - embedded_post dict with:
     - available=False
     - author_username (partial info, per D-06)

4. `original_post_with_media` fixture:
   - post_type='original'
   - media_urls list with 2-3 URLs
   - No embedded_post

Create tests/test_cli_display.py with imports for fixtures. Use existing Rich Console test pattern from other test files in the project.
  </action>
  <verify>
    <automated>pytest tests/test_cli_display.py -v --collect-only 2>&1 | grep -E "test_.*fixture" | wc -l | grep -q "4"</automated>
  </verify>
  <done>Test fixtures exist in tests/conftest.py and are importable in tests/test_cli_display.py</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing tests for quote tweet rendering</name>
  <files>tests/test_cli_display.py</files>
  <behavior>
    - Test 1: display_post with quote tweet produces nested Panel structure
    - Test 2: display_post shows "[dim]Quoting @username[/dim]" attribution above inner panel
    - Test 3: display_post shows quoter's text prominently, quoted content dimmed
    - Test 4: display_post combines media URLs from both quoter and quoted post (per D-08)
  </behavior>
  <read_first>
    - tests/conftest.py (fixtures from Task 1)
    - src/cli/display.py (existing display_post function)
  </read_first>
  <action>
Create test functions in tests/test_cli_display.py for quote tweet rendering per CLI-06 and D-01, D-02, D-08:

1. `test_quote_tweet_nested_panel()`:
   - Call display_post(console, quote_tweet_post, embedded_post=quote_tweet_post['embedded_post'])
   - Capture console output
   - Assert output contains both quoter's author and quoted author
   - Assert nested structure exists (outer panel for quoter, inner content for quoted)

2. `test_quote_tweet_attribution()`:
   - Call display_post with quote tweet
   - Assert output contains "[dim]Quoting @username[/dim]" per D-02
   - Attribution appears above the quoted content

3. `test_quote_tweet_visual_hierarchy()`:
   - Quoter's text appears before quoted text
   - Quoted content has dim border styling per D-01

4. `test_quote_tweet_combined_media_urls()`:
   - Quote post with media_urls from both quoter and quoted
   - Assert all media URLs displayed per D-08
   - No separate section for embedded media

Use Rich Console's capture feature to test output. Tests should FAIL initially (TDD red phase).
  </action>
  <verify>
    <automated>pytest tests/test_cli_display.py::test_quote_tweet_nested_panel -x 2>&1 | grep -E "(FAILED|AssertionError)" | head -1 | grep -q "FAILED"</automated>
  </verify>
  <done>Failing tests for quote tweet rendering exist in tests/test_cli_display.py</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write failing tests for retweet rendering</name>
  <files>tests/test_cli_display.py</files>
  <behavior>
    - Test 1: display_post with retweet shows "Reposted from @username" header
    - Test 2: display_post shows "Reposted by @retweeter" line
    - Test 3: display_post renders original content panel with original author
    - Test 4: display_post shows media URLs from original post (embedded_post.media_urls)
  </behavior>
  <read_first>
    - tests/conftest.py (fixtures from Task 1)
    - src/cli/display.py (existing display_post function)
  </read_first>
  <action>
Create test functions in tests/test_cli_display.py for retweet rendering per CLI-07 and D-03, D-04, D-08:

1. `test_retweet_header()`:
   - Call display_post(console, retweet_post, embedded_post=retweet_post['embedded_post'])
   - Assert output contains "[dim]Reposted from @original_author[/dim]" per D-03
   - Header appears above content

2. `test_retweet_reposter_info()`:
   - Call display_post with retweet
   - Assert output contains "[dim]Reposted by @retweeter[/dim]" per D-04
   - Shows who retweeted while keeping original author prominent

3. `test_retweet_content_panel()`:
   - Original content displayed in standard Panel
   - Original author shown with [bold cyan]@username[/bold cyan]
   - Same styling as original posts

4. `test_retweet_media_urls()`:
   - Retweet with media_urls in embedded_post
   - Assert media URLs displayed with 🔗 prefix per D-07

Tests should FAIL initially (TDD red phase).
  </action>
  <verify>
    <automated>pytest tests/test_cli_display.py::test_retweet_header -x 2>&1 | grep -E "(FAILED|AssertionError)" | head -1 | grep -q "FAILED"</automated>
  </verify>
  <done>Failing tests for retweet rendering exist in tests/test_cli_display.py</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Write failing tests for unavailable post handling</name>
  <files>tests/test_cli_display.py</files>
  <behavior>
    - Test 1: display_post with unavailable embedded_post shows red-bordered placeholder
    - Test 2: display_post shows "Original post unavailable" message
    - Test 3: display_post shows "[dim]Originally by @username[/dim]" when author known but post unavailable (D-06)
  </behavior>
  <read_first>
    - tests/conftest.py (unavailable_post fixture)
    - src/cli/display.py (existing display_post function)
  </read_first>
  <action>
Create test functions in tests/test_cli_display.py for unavailable post handling per D-05, D-06:

1. `test_unavailable_placeholder_panel()`:
   - Call display_post(console, post, embedded_post=unavailable_embedded)
   - Assert Panel has border_style="red" per D-05
   - Placeholder content displayed instead of actual post

2. `test_unavailable_message()`:
   - Unavailable embedded post
   - Assert output contains "Original post unavailable" per D-05

3. `test_unavailable_with_author()`:
   - Embedded post with author info but available=False
   - Assert output contains "[dim]Originally by @username[/dim]" per D-06
   - Shows partial author information when known

Tests should FAIL initially (TDD red phase).
  </action>
  <verify>
    <automated>pytest tests/test_cli_display.py::test_unavailable_placeholder_panel -x 2>&1 | grep -E "(FAILED|AssertionError)" | head -1 | grep -q "FAILED"</automated>
  </verify>
  <done>Failing tests for unavailable post handling exist in tests/test_cli_display.py</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Write failing tests for media URL display</name>
  <files>tests/test_cli_display.py</files>
  <behavior>
    - Test 1: display_post shows media URLs with 🔗 prefix and indentation
    - Test 2: display_post for quote tweet combines both original and embedded media URLs
    - Test 3: display_post for retweet shows embedded media URLs
  </behavior>
  <read_first>
    - tests/conftest.py (original_post_with_media fixture)
    - src/cli/display.py (existing display_post function)
  </read_first>
  <action>
Create test functions in tests/test_cli_display.py for media URL display per CLI-08 and D-07, D-08:

1. `test_media_urls_display()`:
   - Call display_post with post containing media_urls
   - Assert each URL displayed with 🔗 prefix per D-07
   - URLs are indented and dim styled

2. `test_media_urls_quote_combined()`:
   - Quote tweet with media_urls in both post and embedded_post
   - Assert ALL media URLs displayed per D-08
   - Combined list, no separate sections

3. `test_media_urls_retweet()`:
   - Retweet with media_urls in embedded_post only
   - Assert embedded media URLs displayed correctly

Tests should FAIL initially (TDD red phase).
  </action>
  <verify>
    <automated>pytest tests/test_cli_display.py::test_media_urls_display -x 2>&1 | grep -E "(FAILED|AssertionError)" | head -1 | grep -q "FAILED"</automated>
  </verify>
  <done>Failing tests for media URL display exist in tests/test_cli_display.py</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → display_post | Test data simulates post dicts; no untrusted input crosses boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-00-01 | Tampering | Test fixtures | accept | Test fixtures are developer-controlled, not user input |
| T-10-00-02 | Information Disclosure | Console output | accept | Console output for development, no production secrets in test data |
</threat_model>

<verification>
Run all tests to confirm RED state:
```bash
pytest tests/test_cli_display.py -v
```

Expected: All tests FAIL (no implementation yet)

Confirm test collection:
```bash
pytest tests/test_cli_display.py --collect-only
```

Expected: 10+ tests collected (fixtures + test functions)
</verification>

<success_criteria>
- [ ] Test fixtures exist for quote tweet, retweet, unavailable post, and media posts
- [ ] Failing tests exist for quote tweet nested Panel rendering (CLI-06)
- [ ] Failing tests exist for retweet header and content display (CLI-07)
- [ ] Failing tests exist for unavailable post placeholder (D-05, D-06)
- [ ] Failing tests exist for media URL display (CLI-08)
- [ ] All tests in RED state (expected failures before implementation)
- [ ] Tests run in < 5 seconds
</success_criteria>

<output>
After completion, create `.planning/phases/10-cli-display/10-00-SUMMARY.md`
</output>