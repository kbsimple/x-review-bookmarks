---
phase: 08-storage-foundation
plan: 01
type: execute
wave: 1
depends_on: ["08-00"]
files_modified:
  - src/db/schema.py
  - src/db/migrations.py
  - src/api/x_client.py
autonomous: true
requirements:
  - STR-01
  - STR-02
user_setup: []
must_haves:
  truths:
    - "Schema V6 migration creates embedded_posts table"
    - "Schema V6 migration adds post_type and embedded_post_id columns to posts"
    - "XClient.EXPANSIONS includes referenced_tweets.id.* expansions"
    - "XClient.TWEET_FIELDS includes referenced_tweets field"
    - "BookmarkFetchResult includes referenced_tweets dictionary"
  artifacts:
    - path: "src/db/schema.py"
      provides: "SCHEMA_V6_MIGRATION constant"
      contains: "CREATE TABLE IF NOT EXISTS embedded_posts"
    - path: "src/db/migrations.py"
      provides: "migrate_to_v6 function"
      contains: "def migrate_to_v6"
    - path: "src/api/x_client.py"
      provides: "Extended expansions for embedded posts"
      contains: "referenced_tweets.id"
  key_links:
    - from: "src/db/migrations.py"
      to: "src/db/schema.py"
      via: "import SCHEMA_V6_MIGRATION"
    - from: "src/services/sync.py"
      to: "src/api/x_client.py"
      via: "BookmarkFetchResult.referenced_tweets"
---

<objective>
Implement schema migration V6 for embedded posts and extend XClient to fetch referenced tweet content.

Purpose: Database must support embedded post storage and posts must have type classification before sync integration.
Output: SCHEMA_V6_MIGRATION, migrate_to_v6(), extended XClient.EXPANSIONS, BookmarkFetchResult.referenced_tweets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-storage-foundation/08-CONTEXT.md
@.planning/phases/08-storage-foundation/08-RESEARCH.md

<interfaces>
<!-- Existing patterns to follow -->
From src/db/schema.py:
```python
SCHEMA_V5_MIGRATION = """
CREATE TABLE IF NOT EXISTS post_review_state (...)
"""

def get_schema_version() -> str:
    return "v5"
```

From src/db/migrations.py:
```python
def migrate_to_v5(conn: sqlite3.Connection) -> None:
    current_version = get_schema_version_int(conn)
    if current_version >= 5:
        return
    conn.executescript(SCHEMA_V5_MIGRATION)
    conn.execute("PRAGMA user_version = 5")
    conn.commit()
```

From src/api/x_client.py:
```python
EXPANSIONS = "author_id,attachments.media_keys"
TWEET_FIELDS = "created_at,public_metrics,attachments,entities"

class BookmarkFetchResult:
    tweets: list[Any] = field(default_factory=list)
    users: dict[str, Any] = field(default_factory=dict)
    media: dict[str, Any] = field(default_factory=dict)
```

<!-- D-01 columns from CONTEXT.md -->
D-01 columns: x_post_id, created_at, text, author_id, author_username, author_display_name, media_urls, link_urls, available

<!-- D-04 expansions from CONTEXT.md -->
D-04: referenced_tweets.id, referenced_tweets.id.author_id, referenced_tweets.id.attachments.media_keys
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SCHEMA_V6_MIGRATION to schema.py</name>
  <files>src/db/schema.py</files>
  <read_first>
    - src/db/schema.py (current schema patterns)
    - tests/test_migrations.py (expected test behavior)
  </read_first>
  <behavior>
    - Test: test_migrate_to_v6_creates_embedded_posts_table passes
    - Test: test_migrate_to_v6_adds_post_type_column passes
    - Test: test_migrate_to_v6_adds_embedded_post_id_column passes
    - Test: test_get_schema_version_returns_v6 passes
  </behavior>
  <action>Add SCHEMA_V6_MIGRATION constant to src/db/schema.py following existing SCHEMA_V5_MIGRATION pattern. Include:

1. CREATE TABLE IF NOT EXISTS embedded_posts with columns per D-01:
   - x_post_id TEXT PRIMARY KEY
   - created_at TIMESTAMP NOT NULL
   - text TEXT NOT NULL
   - author_id TEXT NOT NULL
   - author_username TEXT NOT NULL
   - author_display_name TEXT
   - media_urls TEXT (JSON array)
   - link_urls TEXT (JSON array)
   - available INTEGER DEFAULT 1 (Boolean: 1=available, 0=deleted/protected per RESEARCH.md)

2. ALTER TABLE posts ADD COLUMN post_type TEXT DEFAULT 'original' (per D-02)

3. ALTER TABLE posts ADD COLUMN embedded_post_id TEXT (per D-02)

Note: SQLite doesn't support ADD COLUMN IF NOT EXISTS, so ALTER statements go in migration function with try/except.

Update get_schema_version() to return "v6".

Run tests: pytest tests/test_migrations.py -v</action>
  <verify>
    <automated>pytest tests/test_migrations.py::TestMigrateToV6::test_migrate_to_v6_creates_embedded_posts_table tests/test_migrations.py::TestMigrateToV6::test_get_schema_version_returns_v6 -v</automated>
  </verify>
  <done>
    - SCHEMA_V6_MIGRATION constant exists with embedded_posts table creation
    - get_schema_version() returns "v6"
    - Tests for schema creation pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add migrate_to_v6 to migrations.py</name>
  <files>src/db/migrations.py</files>
  <read_first>
    - src/db/migrations.py (existing migration pattern)
    - src/db/schema.py (SCHEMA_V6_MIGRATION location)
    - tests/test_migrations.py (expected test behavior)
  </read_first>
  <behavior>
    - Test: test_migrate_to_v6_is_idempotent passes
    - Migration function is idempotent (can be called multiple times)
  </behavior>
  <action>Add migrate_to_v6 function to src/db/migrations.py following existing migrate_to_v5 pattern:

1. Check if current_version >= 6, return early if already migrated
2. Create embedded_posts table using conn.executescript(SCHEMA_V6_MIGRATION)
3. Add post_type column with try/except for idempotency:
   try: conn.execute("ALTER TABLE posts ADD COLUMN post_type TEXT DEFAULT 'original'")
   except sqlite3.OperationalError: pass
4. Add embedded_post_id column with try/except for idempotency:
   try: conn.execute("ALTER TABLE posts ADD COLUMN embedded_post_id TEXT")
   except sqlite3.OperationalError: pass
5. Set PRAGMA user_version = 6
6. conn.commit()

Import SCHEMA_V6_MIGRATION from schema.py.
Add migrate_to_v6 call to run_migrations function.
Add to __all__ export list.

Run tests: pytest tests/test_migrations.py -v</action>
  <verify>
    <automated>pytest tests/test_migrations.py::TestMigrateToV6 -v</automated>
  </verify>
  <done>
    - migrate_to_v6 function exists
    - run_migrations includes migrate_to_v6 call
    - All migration tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend XClient EXPANSIONS and TWEET_FIELDS</name>
  <files>src/api/x_client.py</files>
  <read_first>
    - src/api/x_client.py (current expansions pattern)
    - tests/test_sync_service.py (expected BookmarkFetchResult behavior)
  </read_first>
  <behavior>
    - Test: BookmarkFetchResult includes referenced_tweets dictionary
    - Test: XClient.EXPANSIONS contains referenced_tweets.id
    - Test: XClient.TWEET_FIELDS contains referenced_tweets
  </behavior>
  <action>Modify src/api/x_client.py:

1. Extend EXPANSIONS constant to include referenced_tweets expansions per D-04:
   OLD: EXPANSIONS = "author_id,attachments.media_keys"
   NEW: EXPANSIONS = "author_id,attachments.media_keys,referenced_tweets.id,referenced_tweets.id.author_id,referenced_tweets.id.attachments.media_keys"

2. Extend TWEET_FIELDS to include referenced_tweets:
   OLD: TWEET_FIELDS = "created_at,public_metrics,attachments,entities"
   NEW: TWEET_FIELDS = "created_at,public_metrics,attachments,entities,referenced_tweets"

3. Add referenced_tweets to BookmarkFetchResult dataclass:
   Add field: referenced_tweets: dict[str, Any] = field(default_factory=dict)

4. In fetch_bookmarks method, build referenced_tweets dictionary from includes:
   Add to result construction:
   referenced_tweets={str(t.id): t for t in response.includes.get('tweets', [])} if response.includes else {}

Per RESEARCH.md Pattern 3, referenced tweets appear in includes.tweets when referenced_tweets.id expansion is used. Build dictionary with str(t.id) keys for O(1) lookup.

Run tests: pytest tests/test_sync_service.py -v -k embedded</action>
  <verify>
    <automated>pytest tests/test_sync_service.py::TestEmbeddedPostsSync -v --tb=short</automated>
  </verify>
  <done>
    - EXPANSIONS includes referenced_tweets.id, referenced_tweets.id.author_id, referenced_tweets.id.attachments.media_keys
    - TWEET_FIELDS includes referenced_tweets
    - BookmarkFetchResult has referenced_tweets field
    - fetch_bookmarks populates referenced_tweets from includes.tweets
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Database → Application | SQLite queries use parameterized statements (existing pattern) |
| API → Application | X API responses are untrusted, validate structure before access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Tampering | Schema migration | mitigate | Use IF NOT EXISTS for table creation, try/except for ALTER TABLE |
| T-08-01-02 | Information Disclosure | API fields | accept | referenced_tweets are from user's own bookmarks |
| T-08-01-03 | Tampering | referenced_tweets access | mitigate | Check hasattr(tweet, 'referenced_tweets') and None before accessing |

</threat_model>

<verification>
- All migration tests pass
- XClient.EXPANSIONS includes referenced_tweets.id.*
- XClient.TWEET_FIELDS includes referenced_tweets
- BookmarkFetchResult has referenced_tweets field
- Schema V6 creates embedded_posts table with all required columns
</verification>

<success_criteria>
- Tests in tests/test_migrations.py all pass
- get_schema_version() returns "v6"
- migrate_to_v6 is idempotent
- XClient can fetch referenced tweet content
- referenced_tweets accessible from BookmarkFetchResult
</success_criteria>

<output>
After completion, create `.planning/phases/08-storage-foundation/08-01-SUMMARY.md`
</output>