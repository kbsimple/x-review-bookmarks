---
phase: 11-cast-display
plan: 01
type: execute
wave: 1
depends_on: [11-00]
files_modified: [src/web/templates/receiver.html]
autonomous: true
requirements: [CAST-06, CAST-07]
must_haves:
  truths:
    - "User sees quote tweets with nested card structure on TV"
    - "User sees retweets with 'Reposted from' attribution header on TV"
    - "User sees embedded media full-width within nested cards on TV"
    - "User sees 'Original post unavailable' placeholder for deleted posts on TV"
  artifacts:
    - path: "src/web/templates/receiver.html"
      provides: "Cast receiver HTML with embedded post rendering"
      contains: "post_type === 'quote'"
      min_lines: 200
  key_links:
    - from: "receiver.html"
      to: "loadPost()"
      via: "post_type conditional rendering"
---

<objective>
Update Cast receiver template to render embedded posts (retweets and quote tweets) with TV-optimized visual styling.

Purpose: Display embedded posts on TV with larger text sizes, nested cards for quote tweets, and clear attribution headers for retweets.
Output: Updated receiver.html with conditional rendering for post_type variants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-cast-display/11-CONTEXT.md
@.planning/phases/11-cast-display/11-00-PLAN.md

<!-- Existing receiver template -->
@src/web/templates/receiver.html
</context>

<interfaces>
<!-- Key data structures from prior phases -->

From src/repositories/posts.py (get_paginated_with_embedded):
```python
# Post with embedded_post:
{
    'post_type': 'quote',  # or 'retweet', 'original'
    'embedded_post': {
        'x_post_id': str,
        'text': str,
        'author_username': str,
        'author_display_name': str,
        'media_urls': list[str],
        'available': bool
    }
}
```

From Phase 9 CONTEXT.md (Web Display decisions):
- D-01: Nested card layout for quote tweets (outer card + inner card)
- D-02: "Quoting @username" gray label above nested card
- D-03: "Reposted from @author" gray header for retweets
- D-04: "Reposted by @retweeter" smaller text above retweet header
- D-05: Gray placeholder card for unavailable posts
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CSS styles for nested cards and TV optimization</name>
  <files>src/web/templates/receiver.html</files>
  <behavior>
    - Nested card background: #1a1a1a (distinct from #000 body)
    - Nested card border: 2px solid #333
    - Quote attribution label: 1.8rem gray "Quoting @username"
    - Retweet header: 1.5rem gray "Reposted by @retweeter", 1.8rem gray "Reposted from @author"
    - Unavailable placeholder: 2rem gray "Original post unavailable"
  </behavior>
  <action>Add CSS styles per D-01 through D-09:
- `.quote-card` with #1a1a1a background and #333 border for nested cards
- `.quote-label` with 1.8rem gray text for "Quoting @username"
- `.retweet-header` with gray text for attribution
- `.unavailable-card` with centered gray placeholder
- TV text sizes: 3rem base (per D-01), 2.5rem author names (per D-04)</action>
  <verify>
    <automated>pytest tests/test_cast_receiver.py -v --tb=short 2>&1 | grep -c "PASSED" | grep -q "[1-5]"</automated>
  </verify>
  <done>CSS styles exist for nested cards, attribution headers, and unavailable placeholders</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add quote tweet rendering in loadPost()</name>
  <files>src/web/templates/receiver.html</files>
  <behavior>
    - Quote tweet shows user's commentary in main card
    - "Quoting @embedded_author" label above nested card
    - Nested card shows original post text, author, media
    - Original post unavailable shows placeholder in nested card
  </behavior>
  <action>Extend loadPost() function to handle post_type === 'quote':
- Render user's commentary in main card (existing pattern)
- Add "Quoting @username" label with .quote-label style
- Create nested card with .quote-card class for embedded content
- Render embedded_post.author_username, embedded_post.text, embedded_post.media_urls
- If embedded_post.available === false, render .unavailable-card with placeholder
- Handle missing embedded_post gracefully with null checks</action>
  <verify>
    <automated>pytest tests/test_cast_receiver.py::test_quote_tweet_rendering -v --tb=short 2>&1 | grep -q "PASSED"</automated>
  </verify>
  <done>Quote tweets render with nested card structure on TV</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add retweet rendering in loadPost()</name>
  <files>src/web/templates/receiver.html</files>
  <behavior>
    - Retweet shows "Reposted by @retweeter" smaller line
    - Retweet shows "Reposted from @original_author" header
    - Original author and content displayed prominently
    - Media renders full-width within retweet card
  </behavior>
  <action>Extend loadPost() function to handle post_type === 'retweet':
- Add "Reposted by @retweeter" line with smaller gray text (1.5rem per D-07)
- Add "Reposted from @author" header (1.8rem per D-07)
- Render embedded_post author with same prominence as original posts (2.5rem per D-04, D-08)
- Render embedded_post.text and embedded_post.media_urls with TV sizing
- Same unavailable handling as quote tweets if embedded_post.available === false</action>
  <verify>
    <automated>pytest tests/test_cast_receiver.py::test_retweet_rendering -v --tb=short 2>&1 | grep -q "PASSED"</automated>
  </verify>
  <done>Retweets render with attribution headers on TV</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add unavailable post placeholder rendering</name>
  <files>src/web/templates/receiver.html</files>
  <behavior>
    - Unavailable posts show gray placeholder card
    - "Original post unavailable" message centered
    - If author known, "@username" shown above message
    - Post icon displayed as visual indicator
  </behavior>
  <action>Create renderUnavailablePost() helper function:
- Gray #1a1a1a background card with centered content
- "Original post unavailable" message in 2rem gray text (per D-09)
- If embedded_post.author_username exists, show "@username" above message
- Add generic post icon (emoji or SVG) as visual indicator
- Call from quote tweet and retweet rendering when available === false</action>
  <verify>
    <automated>pytest tests/test_cast_receiver.py::test_unavailable_placeholder -v --tb=short 2>&1 | grep -q "PASSED"</automated>
  </verify>
  <done>Unavailable posts render with TV-friendly placeholder on TV</done>
</task>

</tasks>

<verification>
- [ ] All tests pass in tests/test_cast_receiver.py
- [ ] receiver.html handles post_type: original, retweet, quote
- [ ] Nested cards render for quote tweets
- [ ] Attribution headers render for retweets
- [ ] Unavailable placeholders render for deleted posts
- [ ] TV-optimized styling (3rem text, high contrast)
</verification>

<success_criteria>
Cast receiver displays embedded posts with TV-optimized layout matching CONTEXT.md decisions D-01 through D-10.
</success_criteria>

<output>
After completion, create `.planning/phases/11-cast-display/11-01-SUMMARY.md`
</output>