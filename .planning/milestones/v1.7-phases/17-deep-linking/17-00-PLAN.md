---
phase: 17-deep-linking
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_static_export_service.py
autonomous: true
requirements: [DL-01, DL-02, DL-03, DL-04, DL-05, DL-06, DL-07, DL-08, DL-09, DL-10, DL-11]

must_haves:
  truths:
    - "TestIndexHtmlDeepLink class exists in tests/test_static_export_service.py"
    - "All 11 test stubs are present and fail RED before Wave 1 implementation"
    - "Each stub asserts a unique deep-link string not yet present in index.html"
  artifacts:
    - path: "tests/test_static_export_service.py"
      provides: "TestIndexHtmlDeepLink class with 11 failing test methods"
      contains: "class TestIndexHtmlDeepLink"
  key_links:
    - from: "TestIndexHtmlDeepLink"
      to: "TestIndexHtmlCarousel"
      via: "placed immediately after — same fixture and import pattern"
      pattern: "class TestIndexHtmlDeepLink"
---

<objective>
Create 11 RED failing test stubs in `TestIndexHtmlDeepLink` class — Nyquist compliance for Phase 17 deep linking.

Purpose: Establish the test gates that Wave 1 implementation must satisfy. All 11 stubs must FAIL before any implementation exists.
Output: `TestIndexHtmlDeepLink` class inserted into `tests/test_static_export_service.py` with 11 string-grep tests, all RED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-deep-linking/17-CONTEXT.md
@.planning/phases/17-deep-linking/17-VALIDATION.md
@.planning/phases/17-deep-linking/17-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TestIndexHtmlDeepLink class with 11 RED stubs</name>
  <files>tests/test_static_export_service.py</files>

  <read_first>
    - tests/test_static_export_service.py (read fully — must see TestIndexHtmlCarousel ending at line 351 and TestNetlifyToml starting at line 354; the new class goes between them)
  </read_first>

  <action>
Insert the following `TestIndexHtmlDeepLink` class into `tests/test_static_export_service.py` between the end of `TestIndexHtmlCarousel` (after the blank line following line 351) and `class TestNetlifyToml` (line 354). Place it at line 353, pushing `TestNetlifyToml` down.

Every test follows the identical 5-line pattern used in `TestIndexHtmlCarousel`:
1. `from src.services.static_export import StaticExportService` (local import)
2. `svc = StaticExportService(temp_db_v6)`
3. `svc.export(tmp_path)`
4. `html = (tmp_path / "index.html").read_text()`
5. `assert "STRING" in html`

Insert this exact block:

```python
class TestIndexHtmlDeepLink:
    """Tests for deep linking additions to index.html. DL-01 through DL-11."""

    def test_share_btn_present(self, temp_db_v6, tmp_path):
        """DL-01: share button element present in generated HTML."""
        from src.services.static_export import StaticExportService
        svc = StaticExportService(temp_db_v6)
        svc.export(tmp_path)
        html = (tmp_path / "index.html").read_text()
        assert "share-btn" in html

    def test_copy_deep_link_function_present(self, temp_db_v6, tmp_path):
        """DL-02: copyDeepLink function present in generated HTML."""
        from src.services.static_export import StaticExportService
        svc = StaticExportService(temp_db_v6)
        svc.export(tmp_path)
        html = (tmp_path / "index.html").read_text()
        assert "copyDeepLink" in html

    def test_clipboard_write_present(self, temp_db_v6, tmp_path):
        """DL-03: navigator.clipboard.writeText call present in generated HTML."""
        from src.services.static_export import StaticExportService
        svc = StaticExportService(temp_db_v6)
        svc.export(tmp_path)
        html = (tmp_path / "index.html").read_text()
        assert "navigator.clipboard.writeText" in html

    def test_hash_detection_present(self, temp_db_v6, tmp_path):
        """DL-04: window.location.hash read present in generated HTML."""
        from src.services.static_export import StaticExportService
        svc = StaticExportService(temp_db_v6)
        svc.export(tmp_path)
        html = (tmp_path / "index.html").read_text()
        assert "window.location.hash" in html

    def test_post_hash_prefix_present(self, temp_db_v6, tmp_path):
        """DL-05: #post- prefix string present in generated HTML."""
        from src.services.static_export import StaticExportService
        svc = StaticExportService(temp_db_v6)
        svc.export(tmp_path)
        html = (tmp_path / "index.html").read_text()
        assert "#post-" in html

    def test_deep_link_mode_flag_present(self, temp_db_v6, tmp_path):
        """DL-06: deepLinkMode flag declared in generated HTML."""
        from src.services.static_export import StaticExportService
        svc = StaticExportService(temp_db_v6)
        svc.export(tmp_path)
        html = (tmp_path / "index.html").read_text()
        assert "deepLinkMode" in html

    def test_deep_link_mode_css_class_present(self, temp_db_v6, tmp_path):
        """DL-07: deep-link-mode CSS class present in generated HTML."""
        from src.services.static_export import StaticExportService
        svc = StaticExportService(temp_db_v6)
        svc.export(tmp_path)
        html = (tmp_path / "index.html").read_text()
        assert "deep-link-mode" in html

    def test_xbm_home_btn_present(self, temp_db_v6, tmp_path):
        """DL-08: xbm-home-btn element present in generated HTML."""
        from src.services.static_export import StaticExportService
        svc = StaticExportService(temp_db_v6)
        svc.export(tmp_path)
        html = (tmp_path / "index.html").read_text()
        assert "xbm-home-btn" in html

    def test_go_home_function_present(self, temp_db_v6, tmp_path):
        """DL-09: goHome function present in generated HTML."""
        from src.services.static_export import StaticExportService
        svc = StaticExportService(temp_db_v6)
        svc.export(tmp_path)
        html = (tmp_path / "index.html").read_text()
        assert "goHome" in html

    def test_show_deep_link_error_present(self, temp_db_v6, tmp_path):
        """DL-10: showDeepLinkError function present in generated HTML."""
        from src.services.static_export import StaticExportService
        svc = StaticExportService(temp_db_v6)
        svc.export(tmp_path)
        html = (tmp_path / "index.html").read_text()
        assert "showDeepLinkError" in html

    def test_xbm_home_text_present(self, temp_db_v6, tmp_path):
        """DL-11: 'XBM Home' text present in generated HTML."""
        from src.services.static_export import StaticExportService
        svc = StaticExportService(temp_db_v6)
        svc.export(tmp_path)
        html = (tmp_path / "index.html").read_text()
        assert "XBM Home" in html
```

Do NOT add any import at the top of the file — the existing conftest handles fixtures. Use the local import pattern already established in `TestIndexHtmlCarousel`.
  </action>

  <verify>
    <automated>python3 -m pytest tests/test_static_export_service.py::TestIndexHtmlDeepLink -x -q 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "def test_" tests/test_static_export_service.py` output increases by 11 from its pre-task value
    - `grep "class TestIndexHtmlDeepLink" tests/test_static_export_service.py` returns a match
    - `python3 -m pytest tests/test_static_export_service.py::TestIndexHtmlDeepLink -x -q` exits non-zero (tests FAIL — RED is the goal)
    - `python3 -m pytest tests/test_static_export_service.py::TestIndexHtmlDeepLink -q 2>&1 | grep -E "passed|failed"` shows 11 failed, 0 passed
    - `python3 -m pytest tests/test_static_export_service.py -q` exits with the same pass count as before this task plus 0 new passes (only new failures)
    - The `TestNetlifyToml` class still exists after the insertion (not accidentally deleted)
  </acceptance_criteria>

  <done>TestIndexHtmlDeepLink class exists in tests/test_static_export_service.py with 11 methods, all failing RED. Full suite passes all previously-passing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → production code | Test file reads generated HTML; no external input, no user data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-00-01 | Tampering | test_static_export_service.py | accept | Wave 0 is test-only; no user data or production paths affected |
</threat_model>

<verification>
Run after task:
```bash
python3 -m pytest tests/test_static_export_service.py::TestIndexHtmlDeepLink -q
# Expected: 11 FAILED (RED stubs before implementation)
python3 -m pytest tests/test_static_export_service.py -q
# Expected: existing tests still passing + 11 new failures
```
</verification>

<success_criteria>
- TestIndexHtmlDeepLink class with 11 test methods exists in tests/test_static_export_service.py
- All 11 tests FAIL (RED) — no string they assert is present in the current index.html
- Previously-passing tests are all still passing (no regressions from insertion)
- Class is inserted between TestIndexHtmlCarousel and TestNetlifyToml
</success_criteria>

<output>
After completion, create `.planning/phases/17-deep-linking/17-00-SUMMARY.md`
</output>
