---
phase: 04-topic-organization
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: []
autonomous: true
requirements: []
must_haves:
  truths:
    - "ML dependencies are importable (sentence-transformers, scikit-learn)"
    - "Test stub files exist for all Phase 4 components"
    - "Fixtures for tags, topics, embeddings are available"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project dependencies"
      contains: "sentence-transformers"
    - path: "tests/test_tags_repository.py"
      provides: "Tags repository test stubs"
      min_lines: 20
    - path: "tests/test_topics_repository.py"
      provides: "Topics repository test stubs"
      min_lines: 30
    - path: "tests/conftest.py"
      provides: "Phase 4 fixtures"
      contains: "SCHEMA_V4_TABLES"
  key_links: []
user_setup:
  - service: sentence-transformers
    why: "Text embeddings for topic clustering"
    env_vars: []
    dashboard_config: []
---

<objective>
Establish test infrastructure and install dependencies for Phase 4 (Topic Organization).

Purpose: Ensure testing framework is ready before implementation begins. Install ML dependencies for embeddings.
Output: Test stubs, fixtures, installed dependencies
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-topic-organization/04-RESEARCH.md
@.planning/phases/04-topic-organization/04-VALIDATION.md

# Existing test infrastructure
@tests/conftest.py
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install ML dependencies</name>
  <files>pyproject.toml, requirements.txt</files>
  <read_first>
    - /Users/ffaber/claude-projects/x-bookmarked-posts/pyproject.toml
  </read_first>
  <action>Add sentence-transformers and scikit-learn as project dependencies.

1. Add to pyproject.toml [project.dependencies]:
   - "sentence-transformers>=5.1"
   - "scikit-learn>=1.5"

2. Run `pip install -e .` to install the new dependencies.

3. Verify installation with:
   - `python -c "from sentence_transformers import SentenceTransformer; print('OK')"`
   - `python -c "from sklearn.cluster import KMeans; print('OK')"`
</action>
  <verify>
    <automated>python -c "from sentence_transformers import SentenceTransformer; from sklearn.cluster import KMeans; print('Dependencies OK')"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains "sentence-transformers" in dependencies
    - pyproject.toml contains "scikit-learn" in dependencies
    - `python -c "from sentence_transformers import SentenceTransformer"` succeeds
    - `python -c "from sklearn.cluster import KMeans"` succeeds
  </acceptance_criteria>
  <done>sentence-transformers and scikit-learn installed and importable</done>
</task>

<task type="auto">
  <name>Task 2: Create test stubs for Phase 4</name>
  <files>tests/test_tags_repository.py, tests/test_topics_repository.py, tests/test_embedding_service.py, tests/test_clustering_service.py, tests/test_topic_suggester.py</files>
  <read_first>
    - /Users/ffaber/claude-projects/x-bookmarked-posts/tests/conftest.py
    - /Users/ffaber/claude-projects/x-bookmarked-posts/tests/test_posts_repository.py
  </read_first>
  <action>Create test stub files with pytest structure for Phase 4 components.

Create 5 test files with placeholder tests:

1. tests/test_tags_repository.py:
   - test_get_or_create_tag
   - test_assign_tag_to_post
   - test_remove_tag_from_post
   - test_get_post_tags
   - test_get_posts_by_tag
   (All marked with pytest.mark.skip initially)

2. tests/test_topics_repository.py:
   - test_create_topic
   - test_get_topic_by_id
   - test_list_topics
   - test_update_topic
   - test_delete_topic
   - test_assign_topic_to_post
   - test_get_post_topics
   - test_create_pending_assignment
   - test_approve_pending_assignment
   - test_reject_pending_assignment
   (All marked with pytest.mark.skip initially)

3. tests/test_embedding_service.py:
   - test_generate_embedding
   - test_generate_embeddings_batch
   - test_get_or_create_embedding_from_cache
   - test_embedding_caching
   (All marked with pytest.mark.skip initially)

4. tests/test_clustering_service.py:
   - test_cluster_posts
   - test_compute_topic_centroids
   - test_silhouette_scoring
   (All marked with pytest.mark.skip initially)

5. tests/test_topic_suggester.py:
   - test_suggest_topics_for_post
   - test_suggest_topics_with_threshold
   - test_store_pending_suggestions
   - test_batch_suggest_topics
   (All marked with pytest.mark.skip initially)

Each test file should:
- Import pytest
- Have a docstring describing the test module
- Use pytest.mark.skip on placeholder tests
</action>
  <verify>
    <automated>pytest tests/test_tags_repository.py tests/test_topics_repository.py tests/test_embedding_service.py tests/test_clustering_service.py tests/test_topic_suggester.py --collect-only</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_tags_repository.py exists with 5+ test functions
    - tests/test_topics_repository.py exists with 10+ test functions
    - tests/test_embedding_service.py exists with 4+ test functions
    - tests/test_clustering_service.py exists with 3+ test functions
    - tests/test_topic_suggester.py exists with 4+ test functions
    - All test files import pytest
    - `pytest --collect-only` finds all tests
  </acceptance_criteria>
  <done>Test stub files created with placeholder tests</done>
</task>

<task type="auto">
  <name>Task 3: Update conftest.py with Phase 4 fixtures</name>
  <files>tests/conftest.py</files>
  <read_first>
    - /Users/ffaber/claude-projects/x-bookmarked-posts/tests/conftest.py
    - /Users/ffaber/claude-projects/x-bookmarked-posts/src/db/schema.py
  </read_first>
  <action>Add fixtures for tags, topics, and embeddings tables to conftest.py.

Add the following fixtures to tests/conftest.py:

1. `temp_db_v4` fixture:
   - Extends temp_db pattern
   - Creates tables: tags, post_tags, topics, post_topics, pending_topic_assignments, post_embeddings
   - Use SQL from SCHEMA_V4_MIGRATION in schema.py (will be created in plan 01)

2. `sample_tags` fixture:
   - Returns list of sample tag dicts: [{"id": 1, "name": "python"}, {"id": 2, "name": "ml"}]

3. `sample_topics` fixture:
   - Returns list of sample topic dicts: [{"id": 1, "name": "Programming", "description": "Software development"}, {"id": 2, "name": "Machine Learning", "description": "AI/ML content"}]

4. `sample_post_with_text` fixture:
   - Returns a sample post dict with text content suitable for embedding

Since SCHEMA_V4_MIGRATION does not exist yet, create the fixture with inline SQL for the tables:

```python
SCHEMA_V4_TABLES = """
CREATE TABLE IF NOT EXISTS tags (
    id INTEGER PRIMARY KEY,
    name TEXT UNIQUE NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS post_tags (
    post_id TEXT NOT NULL,
    tag_id INTEGER NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (post_id, tag_id),
    FOREIGN KEY (tag_id) REFERENCES tags(id) ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS topics (
    id INTEGER PRIMARY KEY,
    name TEXT UNIQUE NOT NULL,
    description TEXT,
    parent_id INTEGER,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (parent_id) REFERENCES topics(id) ON DELETE SET NULL
);

CREATE TABLE IF NOT EXISTS post_topics (
    post_id TEXT NOT NULL,
    topic_id INTEGER NOT NULL,
    confidence REAL,
    source TEXT DEFAULT 'user',
    assigned_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (post_id, topic_id),
    FOREIGN KEY (topic_id) REFERENCES topics(id) ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS pending_topic_assignments (
    id INTEGER PRIMARY KEY,
    post_id TEXT NOT NULL,
    topic_id INTEGER NOT NULL,
    confidence REAL NOT NULL,
    suggested_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (topic_id) REFERENCES topics(id) ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS post_embeddings (
    post_id TEXT PRIMARY KEY,
    embedding BLOB NOT NULL,
    model_name TEXT DEFAULT 'all-MiniLM-L6-v2',
    generated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
"""
```

The temp_db_v4 fixture should execute this SQL after creating the base tables.
</action>
  <verify>
    <automated>python -c "from tests.conftest import sample_tags, sample_topics; print('Fixtures OK')"</automated>
  </verify>
  <acceptance_criteria>
    - tests/conftest.py contains `temp_db_v4` fixture
    - tests/conftest.py contains `sample_tags` fixture
    - tests/conftest.py contains `sample_topics` fixture
    - tests/conftest.py contains `sample_post_with_text` fixture
    - SCHEMA_V4_TABLES constant defined with all 6 tables
    - Fixtures are importable from tests.conftest
  </acceptance_criteria>
  <done>conftest.py updated with Phase 4 fixtures</done>
</task>

</tasks>

<verification>
After all tasks:
1. Run `pytest tests/ --collect-only` to verify test discovery
2. Run `python -c "from sentence_transformers import SentenceTransformer; from sklearn.cluster import KMeans"` to verify ML dependencies
3. Run `python -c "from tests.conftest import sample_tags, sample_topics, temp_db_v4"` to verify fixtures
</verification>

<success_criteria>
- sentence-transformers and scikit-learn installed and importable
- 5 test stub files created with placeholder tests
- conftest.py contains fixtures for tags, topics, and embeddings
- pytest can discover all new tests
</success_criteria>

<output>
After completion, create `.planning/phases/04-topic-organization/04-00-SUMMARY.md`
</output>