---
phase: 18-background-prefetch
plan: "00"
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/test_static_export_service.py
autonomous: true
requirements:
  - PREFETCH-01
  - PREFETCH-02
  - PREFETCH-04
  - PREFETCH-05
  - PREFETCH-06
  - PREFETCH-07
  - PREFETCH-08

must_haves:
  truths:
    - "Running pytest on TestIndexHtmlPrefetch produces FAILURES (not errors) before Wave 1"
    - "Six stub test methods exist in TestIndexHtmlPrefetch class"
    - "Each stub asserts a specific JS identifier string that does not yet exist in index.html"
  artifacts:
    - path: "tests/test_static_export_service.py"
      provides: "TestIndexHtmlPrefetch class appended after TestIndexHtmlDeepLink"
      contains: "class TestIndexHtmlPrefetch"
  key_links:
    - from: "tests/test_static_export_service.py::TestIndexHtmlPrefetch"
      to: "src/services/static_export.py (inline JS)"
      via: "svc.export(tmp_path) + (tmp_path / 'index.html').read_text()"
      pattern: "assert.*in html"
---

<objective>
Add RED test stubs for the prefetch pool feature. The `TestIndexHtmlPrefetch` class must be appended to `tests/test_static_export_service.py` following the exact pattern of `TestIndexHtmlCarousel` (line 248) and `TestIndexHtmlDeepLink` (line 354). All six stub methods assert JS identifiers that do not yet exist in the generated `index.html`, so every test MUST FAIL after this plan and PASS after Wave 1.

Purpose: Establish the test contract before implementation so Wave 1 has a concrete green target.
Output: Six failing tests in `tests/test_static_export_service.py::TestIndexHtmlPrefetch`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/18-background-prefetch/18-CONTEXT.md
@.planning/phases/18-background-prefetch/18-PATTERNS.md
@.planning/phases/18-background-prefetch/18-VALIDATION.md

<interfaces>
<!-- Canonical test class pattern extracted from tests/test_static_export_service.py lines 248–351 -->

```python
class TestIndexHtmlCarousel:
    """Tests for carousel mode additions to index.html. VIEWER-01 through VIEWER-05."""

    def test_mode_switcher_button_class_present(self, temp_db_v6, tmp_path):
        """VIEWER-01: mode switcher button class present in generated HTML."""
        from src.services.static_export import StaticExportService
        svc = StaticExportService(temp_db_v6)
        svc.export(tmp_path)
        html = (tmp_path / "index.html").read_text()
        assert "mode-btn" in html

    def test_carousel_render_function_present(self, temp_db_v6, tmp_path):
        """VIEWER-03: renderCarousel function and carouselIndex variable present."""
        from src.services.static_export import StaticExportService
        svc = StaticExportService(temp_db_v6)
        svc.export(tmp_path)
        html = (tmp_path / "index.html").read_text()
        assert "renderCarousel" in html
        assert "carouselIndex" in html
```

<!-- TestIndexHtmlDeepLink ends at line ~444. TestNetlifyToml starts at line 447. -->
<!-- TestIndexHtmlPrefetch is appended BETWEEN TestIndexHtmlDeepLink and TestNetlifyToml. -->
<!-- Fixture signatures: (self, temp_db_v6, tmp_path) — both are pytest fixtures in conftest.py -->
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Add TestIndexHtmlPrefetch RED stubs</name>
  <files>tests/test_static_export_service.py</files>

  <read_first>
    - tests/test_static_export_service.py — read lines 248–280 for exact test class shape, lines 440–470 for insertion point (after TestIndexHtmlDeepLink, before TestNetlifyToml)
  </read_first>

  <behavior>
    Six test methods that FAIL (AssertionError) before Wave 1 implementation, PASS after:

    - test_schedule_prefetch_function_present: assert "schedulePrefetch" in html
      (PREFETCH-01, PREFETCH-08)
    - test_prefetch_window_constants_present: assert "PREFETCH_AHEAD" in html AND "PREFETCH_BEHIND" in html
      (PREFETCH-02)
    - test_request_idle_callback_present: assert "requestIdleCallback" in html
      (PREFETCH-04)
    - test_prefetch_pool_hit_check_present: assert "prefetchPool.has" in html
      (PREFETCH-05)
    - test_clear_prefetch_pool_present: assert "clearPrefetchPool" in html
      (PREFETCH-06)
    - test_prefetch_pool_eviction_present: assert "prefetchPool.delete" in html
      (PREFETCH-07)
  </behavior>

  <action>
Append the following class to `tests/test_static_export_service.py` AFTER the closing method of `TestIndexHtmlDeepLink` and BEFORE `class TestNetlifyToml`. Insert a single blank line before the class and after it (matching surrounding spacing).

```python
class TestIndexHtmlPrefetch:
    """Tests for prefetch pool additions to index.html. PREFETCH-01 through PREFETCH-08."""

    def test_schedule_prefetch_function_present(self, temp_db_v6, tmp_path):
        """PREFETCH-01, PREFETCH-08: schedulePrefetch function present in generated HTML."""
        from src.services.static_export import StaticExportService
        svc = StaticExportService(temp_db_v6)
        svc.export(tmp_path)
        html = (tmp_path / "index.html").read_text()
        assert "schedulePrefetch" in html

    def test_prefetch_window_constants_present(self, temp_db_v6, tmp_path):
        """PREFETCH-02: PREFETCH_AHEAD and PREFETCH_BEHIND constants present in generated HTML."""
        from src.services.static_export import StaticExportService
        svc = StaticExportService(temp_db_v6)
        svc.export(tmp_path)
        html = (tmp_path / "index.html").read_text()
        assert "PREFETCH_AHEAD" in html
        assert "PREFETCH_BEHIND" in html

    def test_request_idle_callback_present(self, temp_db_v6, tmp_path):
        """PREFETCH-04: requestIdleCallback present in generated HTML."""
        from src.services.static_export import StaticExportService
        svc = StaticExportService(temp_db_v6)
        svc.export(tmp_path)
        html = (tmp_path / "index.html").read_text()
        assert "requestIdleCallback" in html

    def test_prefetch_pool_hit_check_present(self, temp_db_v6, tmp_path):
        """PREFETCH-05: prefetchPool.has pool-hit check present in generated HTML."""
        from src.services.static_export import StaticExportService
        svc = StaticExportService(temp_db_v6)
        svc.export(tmp_path)
        html = (tmp_path / "index.html").read_text()
        assert "prefetchPool.has" in html

    def test_clear_prefetch_pool_present(self, temp_db_v6, tmp_path):
        """PREFETCH-06: clearPrefetchPool call present in generated HTML."""
        from src.services.static_export import StaticExportService
        svc = StaticExportService(temp_db_v6)
        svc.export(tmp_path)
        html = (tmp_path / "index.html").read_text()
        assert "clearPrefetchPool" in html

    def test_prefetch_pool_eviction_present(self, temp_db_v6, tmp_path):
        """PREFETCH-07: prefetchPool.delete eviction call present in generated HTML."""
        from src.services.static_export import StaticExportService
        svc = StaticExportService(temp_db_v6)
        svc.export(tmp_path)
        html = (tmp_path / "index.html").read_text()
        assert "prefetchPool.delete" in html
```

Do NOT modify any existing class or test. Append only.
  </action>

  <verify>
    <automated>
      python -m pytest tests/test_static_export_service.py::TestIndexHtmlPrefetch -x -q 2>&1 | tail -5
    </automated>
    Expected: 6 FAILED (AssertionError on each assert — identifier not yet in generated HTML).
    All failures must be AssertionError, not SyntaxError or ImportError.
    Existing tests must still pass:
    python -m pytest tests/test_static_export_service.py -x -q --ignore-glob="*TestIndexHtmlPrefetch*" 2>&1 | tail -3
    (or run full file and confirm only TestIndexHtmlPrefetch tests fail)
  </verify>

  <acceptance_criteria>
    - grep -n "class TestIndexHtmlPrefetch" tests/test_static_export_service.py → prints exactly one line
    - grep -c "def test_" tests/test_static_export_service.py → count increases by 6 versus pre-task count
    - python -m pytest tests/test_static_export_service.py::TestIndexHtmlPrefetch -q 2>&1 | grep "6 failed" → exits with "6 failed"
    - python -m pytest tests/test_static_export_service.py::TestIndexHtmlCarousel -q 2>&1 | grep "passed" → still passes (no regression)
    - python -m pytest tests/test_static_export_service.py::TestIndexHtmlDeepLink -q 2>&1 | grep "passed" → still passes (no regression)
  </acceptance_criteria>

  <done>Six new test methods exist in TestIndexHtmlPrefetch; all six FAIL with AssertionError; no existing tests regressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → generated HTML | Tests read generated file as text; no user input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-00-01 | Tampering | test file | accept | Test file is developer-controlled; no user input; low-value target in test environment |
</threat_model>

<verification>
After this plan: `python -m pytest tests/test_static_export_service.py -x -q` shows exactly 6 failures, all in TestIndexHtmlPrefetch, all AssertionError. All other tests pass.
</verification>

<success_criteria>
- `class TestIndexHtmlPrefetch` present in tests/test_static_export_service.py
- Exactly 6 test methods in the class
- All 6 fail with AssertionError (not syntax or import errors)
- All pre-existing tests in the file still pass
</success_criteria>

<output>
After completion, create `.planning/phases/18-background-prefetch/18-00-SUMMARY.md`
</output>
