---
phase: 11-cast-display
plan: 00
type: tdd
wave: 0
depends_on: []
files_modified: [tests/test_cast_receiver.py]
autonomous: true
requirements: [CAST-06, CAST-07, CAST-08]
must_haves:
  truths:
    - "Test fixtures exist for all post_type variants (original, retweet, quote)"
    - "Test fixtures include embedded post data with available=true and available=false"
    - "Tests verify receiver renders embedded posts correctly"
  artifacts:
    - path: "tests/test_cast_receiver.py"
      provides: "Test fixtures and test cases for cast receiver rendering"
      min_lines: 50
  key_links:
    - from: "tests/test_cast_receiver.py"
      to: "src/web/templates/receiver.html"
      via: "HTML fixture assertions"
---

<objective>
Create test scaffolding with fixtures for post_type variants (original, retweet, quote) and embedded post data to drive TDD for cast receiver embedded post rendering.

Purpose: Establish test-first approach with fixtures for all display cases before implementation.
Output: Test file with fixtures covering original posts, retweets, quote tweets, and unavailable embedded posts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-cast-display/11-CONTEXT.md

<!-- Prior phase patterns -->
@.planning/phases/09-web-display/09-00-SUMMARY.md
</context>

<interfaces>
<!-- Key data structures from existing codebase -->

From src/repositories/posts.py:
```python
# Post data structure from get_paginated_with_embedded():
{
    'x_post_id': str,
    'text': str,
    'author_username': str,
    'author_display_name': str,
    'created_at': str,
    'media_urls': list[str],
    'post_type': 'original' | 'retweet' | 'quote',
    'embedded_post': None | {
        'x_post_id': str,
        'text': str,
        'author_username': str,
        'author_display_name': str,
        'created_at': str,
        'media_urls': list[str],
        'available': bool
    }
}
```

From src/web/static/js/cast.js:
```javascript
// Message format for LOAD_POST:
{
    type: 'LOAD_POST',
    postId: string,
    post: {
        author_username: string,
        text: string,
        created_at: string,
        media_urls: string[],
        topics: {name: string}[],
        post_type: 'original' | 'retweet' | 'quote',
        embedded_post: object | null
    }
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create test file with post fixtures</name>
  <files>tests/test_cast_receiver.py</files>
  <action>Create test file with pytest fixtures for all post_type variants. Include fixtures for:
- Original post (no embedded_post)
- Retweet with available embedded post
- Quote tweet with available embedded post  
- Quote tweet with unavailable embedded post (available=false)
- Post with media URLs

Each fixture should match the data structure from PostsRepository.get_paginated_with_embedded().</action>
  <verify>
    <automated>pytest tests/test_cast_receiver.py -v --collect-only | grep "test_" | wc -l | grep -q "5"</automated>
  </verify>
  <done>Test file exists with fixtures for original, retweet, quote (available), quote (unavailable), and media posts</done>
</task>

<task type="auto">
  <name>Task 2: Create failing tests for embedded post rendering</name>
  <files>tests/test_cast_receiver.py</files>
  <action>Add failing test cases that verify:
- T1: Original post renders without nested card
- T2: Retweet renders with "Reposted from" header
- T3: Quote tweet renders with nested card and "Quoting" label
- T4: Unavailable embedded post renders placeholder with message
- T5: Media in embedded posts renders within nested card

Tests should check HTML output contains expected elements per D-01 through D-10.</action>
  <verify>
    <automated>pytest tests/test_cast_receiver.py -v 2>&1 | grep -c "FAILED" | grep -q "5"</automated>
  </verify>
  <done>5 failing tests exist for embedded post rendering cases</done>
</task>

</tasks>

<verification>
- [ ] Test file exists at tests/test_cast_receiver.py
- [ ] Fixtures cover all post_type variants
- [ ] Failing tests for each rendering case
- [ ] Tests reference CONTEXT.md decisions D-01 through D-10
</verification>

<success_criteria>
Tests exist for all embedded post display cases, ready to drive TDD implementation in subsequent plans.
</success_criteria>

<output>
After completion, create `.planning/phases/11-cast-display/11-00-SUMMARY.md`
</output>