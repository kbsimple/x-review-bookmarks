---
phase: 08-storage-foundation
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - src/services/sync.py
autonomous: true
requirements:
  - STR-01
  - STR-02
  - STR-03
user_setup: []
must_haves:
  truths:
    - "SyncService extracts referenced_tweets from tweet data"
    - "SyncService determines post_type (original, retweet, quote)"
    - "SyncService stores embedded posts with available=True when found"
    - "SyncService stores embedded posts with available=False when not found"
    - "Posts table has post_type and embedded_post_id populated"
    - "Embedded posts are queryable from embedded_posts table"
  artifacts:
    - path: "src/services/sync.py"
      provides: "Embedded post extraction and storage"
      contains: "def _store_tweet"
      contains: "def _store_embedded_post"
      contains: "embedded_post_id"
  key_links:
    - from: "src/services/sync.py"
      to: "src/repositories/embedded_posts.py"
      via: "EmbeddedPostsRepository"
    - from: "src/services/sync.py"
      to: "src/api/x_client.py"
      via: "BookmarkFetchResult.referenced_tweets"
---

<objective>
Integrate embedded post storage into SyncService to extract and store referenced tweet content during sync.

Purpose: Sync is the entry point for all bookmark data. SyncService must detect post type, extract embedded posts, and handle unavailable originals.
Output: Modified SyncService._store_tweet with post_type classification, embedded post storage, and unavailable handling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-storage-foundation/08-CONTEXT.md
@.planning/phases/08-storage-foundation/08-RESEARCH.md

<interfaces>
<!-- Existing SyncService pattern -->
From src/services/sync.py:
```python
class SyncService:
    def __init__(self, access_token: str, conn: sqlite3.Connection, ...):
        self._posts_repo = PostsRepository(conn)
        self._sync_state_repo = SyncStateRepository(conn)
        self._conn = conn

    def _store_tweet(self, tweet: Any, fetch_result: BookmarkFetchResult) -> None:
        author = fetch_result.users.get(tweet.author_id)
        # ... extract data
        self._posts_repo.upsert_post(post)
```

<!-- D-02: post_type values -->
post_type values: 'original', 'retweet', 'quoted' (per D-02: 'original', 'retweet', 'quote')

<!-- D-05: unavailable detection -->
D-05: Check if referenced_id exists in fetch_result.referenced_tweets dict
- If present: available=True
- If missing: available=False, store placeholder embedded post

<!-- D-06: Extend _store_tweet -->
D-06: Extract referenced_tweets, look up in includes, store embedded_post, update posts with post_type and embedded_post_id

<!-- From RESEARCH.md code example -->
```python
if hasattr(tweet, 'referenced_tweets') and tweet.referenced_tweets:
    ref = tweet.referenced_tweets[0]
    ref_type = ref.type  # 'retweeted', 'quoted', 'replied_to'
    ref_id = str(ref.id)
    if ref_type in ('retweeted', 'quoted'):
        post_type = 'retweeted' if ref_type == 'retweeted' else 'quoted'
        embedded_post_id = ref_id
        ref_tweet = fetch_result.referenced_tweets.get(ref_id)
        if ref_tweet:
            self._store_embedded_post(ref_tweet, fetch_result, available=True)
        else:
            self._store_unavailable_embedded_post(ref_id)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add EmbeddedPostsRepository to SyncService initialization</name>
  <files>src/services/sync.py</files>
  <read_first>
    - src/services/sync.py (current initialization pattern)
    - src/repositories/embedded_posts.py (new repository)
    - tests/test_sync_service.py (expected test behavior)
  </read_first>
  <behavior>
    - Test: SyncService has embedded_posts_repo attribute
    - Test: EmbeddedPostsRepository is initialized with connection
  </behavior>
  <action>Modify src/services/sync.py:

1. Import EmbeddedPostsRepository at top of file:
   from ..repositories.embedded_posts import EmbeddedPostsRepository

2. In SyncService.__init__, add embedded_posts_repo:
   self._embedded_posts_repo = EmbeddedPostsRepository(conn)

3. Ensure EmbeddedPostsRepository is imported and instantiated following same pattern as PostsRepository

Run tests to verify import works: pytest tests/test_sync_service.py -v -k embedded --collect-only</action>
  <verify>
    <automated>python -c "from src.services.sync import SyncService; print('import ok')"</automated>
  </verify>
  <done>
    - EmbeddedPostsRepository imported in sync.py
    - SyncService.__init__ creates EmbeddedPostsRepository instance
    - No import errors
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add _store_embedded_post helper method</name>
  <files>src/services/sync.py</files>
  <read_first>
    - src/services/sync.py (_store_tweet pattern)
    - src/repositories/embedded_posts.py (upsert_embedded_post signature)
    - tests/test_sync_service.py (expected test behavior)
  </read_first>
  <behavior>
    - Test: _store_embedded_post creates embedded_posts row with available=True
    - Test: Embedded post has all required fields (text, author, media, links)
  </behavior>
  <action>Add _store_embedded_post helper method to SyncService:

1. Create _store_embedded_post(self, tweet: Any, fetch_result: BookmarkFetchResult, available: bool = True) method

2. Extract embedded post data following same pattern as _store_tweet:
   - x_post_id: str(tweet.id)
   - created_at: tweet.created_at.isoformat() if hasattr(tweet, 'created_at') and tweet.created_at else None
   - text: tweet.text
   - author_id: str(tweet.author_id) if tweet.author_id else ''
   - author_username: author.username if author else '' (look up from fetch_result.users)
   - author_display_name: author.name if author else ''
   - media_urls: [m.url for m in media if hasattr(m, 'url')] (same pattern as _store_tweet)
   - link_urls: self._extract_links(tweet)
   - available: available (parameter)

3. Call self._embedded_posts_repo.upsert_embedded_post(embedded_post)

4. Note: This method is for available embedded posts (found in includes.tweets)

Run tests: pytest tests/test_sync_service.py::TestEmbeddedPostsSync::test_store_tweet_stores_embedded_post_content -v</action>
  <verify>
    <automated>pytest tests/test_sync_service.py::TestEmbeddedPostsSync::test_store_tweet_stores_embedded_post_content -v --tb=short</automated>
  </verify>
  <done>
    - _store_embedded_post method exists
    - Extracts all fields from referenced tweet
    - Calls EmbeddedPostsRepository.upsert_embedded_post
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add _store_unavailable_embedded_post helper method</name>
  <files>src/services/sync.py</files>
  <read_first>
    - src/services/sync.py (_store_embedded_post pattern)
    - tests/test_sync_service.py (expected test behavior for unavailable)
  </read_first>
  <behavior>
    - Test: _store_unavailable_embedded_post creates embedded_posts row with available=False
    - Test: Unavailable embedded post has x_post_id but empty placeholder fields
  </behavior>
  <action>Add _store_unavailable_embedded_post helper method to SyncService for STR-03:

1. Create _store_unavailable_embedded_post(self, x_post_id: str) method

2. Create embedded post dict with minimal data (per D-05):
   - x_post_id: x_post_id (parameter)
   - created_at: None (unknown)
   - text: '' (empty placeholder)
   - author_id: '' (empty placeholder)
   - author_username: '' (empty placeholder)
   - author_display_name: None
   - media_urls: []
   - link_urls: []
   - available: False (INTEGER 0, per RESEARCH.md)

3. Call self._embedded_posts_repo.upsert_embedded_post(embedded_post)

4. Purpose: STR-03 requires handling deleted/protected originals gracefully
   Store placeholder row so embedded_post_id foreign key is valid

Run tests: pytest tests/test_sync_service.py::TestEmbeddedPostsSync::test_store_tweet_handles_unavailable_embedded -v</action>
  <verify>
    <automated>pytest tests/test_sync_service.py::TestEmbeddedPostsSync::test_store_tweet_handles_unavailable_embedded -v --tb=short</automated>
  </verify>
  <done>
    - _store_unavailable_embedded_post method exists
    - Creates embedded_posts row with available=False
    - Has x_post_id but empty/None for other fields
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Modify _store_tweet to handle referenced_tweets</name>
  <files>src/services/sync.py</files>
  <read_first>
    - src/services/sync.py (_store_tweet existing implementation)
    - tests/test_sync_service.py (expected test behavior for post_type)
    - .planning/phases/08-storage-foundation/08-CONTEXT.md (D-06 locked decision)
  </read_first>
  <behavior>
    - Test: test_store_tweet_sets_post_type_original passes
    - Test: test_store_tweet_sets_post_type_retweet passes
    - Test: test_store_tweet_sets_post_type_quote passes
    - Test: test_store_tweet_handles_unavailable_embedded passes
    - Test: test_store_tweet_stores_embedded_post_content passes
  </behavior>
  <action>Modify SyncService._store_tweet to implement D-06:

1. Initialize post_type = 'original' and embedded_post_id = None at start

2. Check for referenced_tweets (per D-06):
   if hasattr(tweet, 'referenced_tweets') and tweet.referenced_tweets:
       ref = tweet.referenced_tweets[0]  # D-03: flatten to 1 level
       ref_type = ref.type  # 'retweeted', 'quoted', 'replied_to'
       ref_id = str(ref.id)

       if ref_type in ('retweeted', 'quoted'):
           # Set post_type per D-02
           post_type = 'retweeted' if ref_type == 'retweeted' else 'quoted'
           embedded_post_id = ref_id

           # Look up referenced tweet in includes per D-05
           ref_tweet = fetch_result.referenced_tweets.get(ref_id)

           if ref_tweet:
               # STR-01: Store embedded post with full content
               self._store_embedded_post(ref_tweet, fetch_result, available=True)
           else:
               # STR-03: Handle deleted/protected original
               self._store_unavailable_embedded_post(ref_id)

3. Add post_type and embedded_post_id to post dict:
   post = {
       'x_post_id': str(tweet.id),
       'created_at': ...,
       'text': tweet.text,
       'author_id': ...,
       'author_username': ...,
       'author_display_name': ...,
       'media_urls': ...,
       'link_urls': ...,
       'bookmarked_at': None,
       'post_type': post_type,        # NEW per D-02
       'embedded_post_id': embedded_post_id,  # NEW per D-02
   }

4. Update PostsRepository.upsert_post call (existing)

Note: Per D-03, only process first referenced_tweets entry. Ignore 'replied_to' type.

Run all embedded sync tests: pytest tests/test_sync_service.py::TestEmbeddedPostsSync -v</action>
  <verify>
    <automated>pytest tests/test_sync_service.py::TestEmbeddedPostsSync -v</automated>
  </verify>
  <done>
    - _store_tweet determines post_type correctly
    - _store_tweet sets embedded_post_id for retweets/quotes
    - _store_tweet calls _store_embedded_post when referenced tweet found
    - _store_tweet calls _store_unavailable_embedded_post when referenced tweet missing
    - All 5 embedded sync tests pass
  </done>
</task>

<task type="auto">
  <name>Task 5: Update PostsRepository.upsert_post for new columns</name>
  <files>src/repositories/posts.py</files>
  <read_first>
    - src/repositories/posts.py (current upsert_post implementation)
    - src/db/schema.py (SCHEMA_V6_MIGRATION columns)
  </read_first>
  <acceptance_criteria>
    - upsert_post includes post_type column with default 'original'
    - upsert_post includes embedded_post_id column (nullable)
    - ON CONFLICT DO UPDATE includes post_type and embedded_post_id
    - _row_to_dict includes post_type and embedded_post_id
  </acceptance_criteria>
  <action>Modify src/repositories/posts.py to handle new columns per D-02:

1. Update upsert_post INSERT statement to include post_type and embedded_post_id:
   INSERT INTO posts (
       x_post_id, created_at, text, author_id, author_username,
       author_display_name, media_urls, link_urls, bookmarked_at, note,
       post_type, embedded_post_id
   ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)

2. Update ON CONFLICT DO UPDATE to include new columns:
   post_type = excluded.post_type,
   embedded_post_id = excluded.embedded_post_id,

3. Update parameter tuple to include new values:
   post.get('post_type', 'original'),
   post.get('embedded_post_id'),

4. Update _row_to_dict to include new fields:
   'post_type': row['post_type'] if 'post_type' in row.keys() else 'original',
   'embedded_post_id': row['embedded_post_id'] if 'embedded_post_id' in row.keys() else None,

Run tests: pytest tests/test_posts_repository.py -v (if tests exist) or pytest tests/ -v -k posts</action>
  <verify>
    <automated>pytest tests/ -v -k posts --tb=short</automated>
  </verify>
  <done>
    - upsert_post handles post_type column
    - upsert_post handles embedded_post_id column
    - _row_to_dict returns post_type and embedded_post_id
    - Default value 'original' for post_type when not provided
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → SyncService | X API responses validated before processing |
| SyncService → Repository | Internal call, validated input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Tampering | referenced_tweets access | mitigate | Check hasattr(tweet, 'referenced_tweets') and tweet.referenced_tweets before accessing |
| T-08-03-02 | Information Disclosure | Unavailable post placeholder | accept | Empty placeholder fields (text='', author='') are safe |
| T-08-03-03 | Tampering | post_type validation | accept | post_type is internal enum, not user input |

</threat_model>

<verification>
- All tests in tests/test_sync_service.py::TestEmbeddedPostsSync pass
- SyncService stores embedded posts for retweets/quotes
- SyncService handles unavailable originals gracefully
- Posts table has post_type and embedded_post_id populated
- PostsRepository.upsert_post handles new columns
</verification>

<success_criteria>
- _store_tweet correctly determines post_type (original, retweet, quote)
- _store_tweet stores embedded posts via EmbeddedPostsRepository
- _store_tweet handles unavailable originals with available=False
- PostsRepository includes post_type and embedded_post_id in upsert
- All Phase 8 tests pass (migrations, embedded_posts_repo, sync)
</success_criteria>

<output>
After completion, create `.planning/phases/08-storage-foundation/08-03-SUMMARY.md`
</output>