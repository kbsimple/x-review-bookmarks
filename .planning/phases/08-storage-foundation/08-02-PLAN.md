---
phase: 08-storage-foundation
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/repositories/embedded_posts.py
autonomous: true
requirements:
  - STR-01
  - STR-03
user_setup: []
must_haves:
  truths:
    - "EmbeddedPostsRepository can upsert embedded posts to database"
    - "EmbeddedPostsRepository can retrieve embedded posts by x_post_id"
    - "Embedded posts are stored with available flag (True/False)"
    - "Upsert handles conflict on x_post_id (insert or update)"
  artifacts:
    - path: "src/repositories/embedded_posts.py"
      provides: "CRUD operations for embedded_posts table"
      exports: ["EmbeddedPostsRepository", "upsert_embedded_post", "get_by_id"]
  key_links:
    - from: "src/services/sync.py"
      to: "src/repositories/embedded_posts.py"
      via: "EmbeddedPostsRepository(conn)"
---

<objective>
Create EmbeddedPostsRepository for CRUD operations on embedded_posts table.

Purpose: Separate repository for embedded post storage follows established repository pattern from PostsRepository.
Output: EmbeddedPostsRepository class with upsert_embedded_post, get_by_id methods.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-storage-foundation/08-CONTEXT.md
@.planning/phases/08-storage-foundation/08-RESEARCH.md

<interfaces>
<!-- Existing PostsRepository pattern to follow -->
From src/repositories/posts.py:
```python
class PostsRepository:
    def __init__(self, conn: sqlite3.Connection):
        self._conn = conn

    def upsert_post(self, post: dict[str, Any]) -> None:
        self._conn.execute(
            """INSERT INTO posts (...) VALUES (...)
               ON CONFLICT(x_post_id) DO UPDATE SET
               ... = excluded....""",
            (...)
        )
        self._conn.commit()

    def _row_to_dict(self, row: sqlite3.Row) -> dict[str, Any]:
        return {
            'x_post_id': row['x_post_id'],
            ...
        }
```

<!-- Schema from Plan 01 -->
embedded_posts columns: x_post_id, created_at, text, author_id, author_username, author_display_name, media_urls, link_urls, available

<!-- D-01 columns from CONTEXT.md -->
D-01: x_post_id TEXT PRIMARY KEY, created_at TIMESTAMP NOT NULL, text TEXT NOT NULL, author_id TEXT NOT NULL, author_username TEXT NOT NULL, author_display_name TEXT, media_urls TEXT, link_urls TEXT, available INTEGER DEFAULT 1
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create EmbeddedPostsRepository with upsert_embedded_post</name>
  <files>src/repositories/embedded_posts.py</files>
  <read_first>
    - src/repositories/posts.py (pattern to follow)
    - src/db/schema.py (SCHEMA_V6_MIGRATION columns)
    - tests/test_embedded_posts_repository.py (expected test behavior)
  </read_first>
  <behavior>
    - Test: test_upsert_embedded_post_inserts_new passes
    - Test: test_upsert_embedded_post_updates_existing passes
    - Test: test_upsert_embedded_post_handles_unavailable passes
  </behavior>
  <action>Create src/repositories/embedded_posts.py with EmbeddedPostsRepository class following PostsRepository pattern:

1. Class definition with __init__(self, conn: sqlite3.Connection)

2. upsert_embedded_post(self, post: dict[str, Any]) -> None method:
   - INSERT INTO embedded_posts with all columns per D-01
   - ON CONFLICT(x_post_id) DO UPDATE SET
     - created_at = excluded.created_at
     - text = excluded.text
     - author_id = excluded.author_id
     - author_username = excluded.author_username
     - author_display_name = excluded.author_display_name
     - media_urls = excluded.media_urls
     - link_urls = excluded.link_urls
     - available = excluded.available
   - Use json.dumps for media_urls and link_urls (TEXT fields)
   - conn.commit() after execute

3. _row_to_dict(self, row: sqlite3.Row) -> dict[str, Any] method:
   - Convert database row to dict
   - Use json.loads for media_urls and link_urls
   - Convert available INTEGER to bool

4. Add module docstring explaining purpose (STR-01: embedded post storage)

Run tests: pytest tests/test_embedded_posts_repository.py -v</action>
  <verify>
    <automated>pytest tests/test_embedded_posts_repository.py::TestEmbeddedPostsRepository::test_upsert_embedded_post_inserts_new tests/test_embedded_posts_repository.py::TestEmbeddedPostsRepository::test_upsert_embedded_post_updates_existing tests/test_embedded_posts_repository.py::TestEmbeddedPostsRepository::test_upsert_embedded_post_handles_unavailable -v</automated>
  </verify>
  <done>
    - EmbeddedPostsRepository class exists
    - upsert_embedded_post method handles INSERT ON CONFLICT DO UPDATE
    - unavailable flag (available=False) is stored as INTEGER 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add get_by_id method to EmbeddedPostsRepository</name>
  <files>src/repositories/embedded_posts.py</files>
  <read_first>
    - src/repositories/posts.py (get_by_id pattern)
    - tests/test_embedded_posts_repository.py (expected test behavior)
  </read_first>
  <behavior>
    - Test: test_get_by_id_returns_dict passes
    - Test: test_get_by_id_returns_none_for_nonexistent passes
  </behavior>
  <action>Add get_by_id method to EmbeddedPostsRepository:

1. get_by_id(self, x_post_id: str) -> Optional[dict[str, Any]]:
   - SELECT * FROM embedded_posts WHERE x_post_id = ?
   - Return None if row is None
   - Return _row_to_dict(row) if found

2. Add to class following same pattern as PostsRepository.get_by_id

3. Ensure returned dict has keys: x_post_id, created_at, text, author_id, author_username, author_display_name, media_urls, link_urls, available

4. Ensure available is bool (converted from INTEGER during _row_to_dict)

Run tests: pytest tests/test_embedded_posts_repository.py -v</action>
  <verify>
    <automated>pytest tests/test_embedded_posts_repository.py::TestEmbeddedPostsRepository::test_get_by_id_returns_dict tests/test_embedded_posts_repository.py::TestEmbeddedPostsRepository::test_get_by_id_returns_none_for_nonexistent -v</automated>
  </verify>
  <done>
    - get_by_id method exists
    - Returns dict with all columns when found
    - Returns None for non-existent x_post_id
    - available field is bool in returned dict
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Application → Database | Repository uses parameterized queries (existing pattern) |
| API → Repository | Input dict is untrusted, validate before use |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Tampering | SQL injection | mitigate | Use parameterized queries (existing pattern from PostsRepository) |
| T-08-02-02 | Tampering | Dict input validation | accept | Callers are internal (SyncService), not external API |

</threat_model>

<verification>
- All tests in tests/test_embedded_posts_repository.py pass
- EmbeddedPostsRepository follows PostsRepository pattern
- Upsert handles conflict correctly
- get_by_id returns None for non-existent posts
- available field is bool in returned dict
</verification>

<success_criteria>
- EmbeddedPostsRepository class implemented
- upsert_embedded_post creates new rows
- upsert_embedded_post updates existing rows on conflict
- get_by_id retrieves embedded posts correctly
- All 5 repository tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-storage-foundation/08-02-SUMMARY.md`
</output>