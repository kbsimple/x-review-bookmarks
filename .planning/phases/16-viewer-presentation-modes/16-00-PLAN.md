---
phase: 16-viewer-presentation-modes
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_static_export_service.py
autonomous: true
requirements:
  - VIEWER-01
  - VIEWER-02
  - VIEWER-03
  - VIEWER-04
  - VIEWER-05

must_haves:
  truths:
    - "TestIndexHtmlCarousel class exists in tests/test_static_export_service.py"
    - "Running TestIndexHtmlCarousel causes all stubs to FAIL before any implementation"
    - "Existing TestIndexHtml tests still pass (no regressions from adding new class)"
  artifacts:
    - path: "tests/test_static_export_service.py"
      provides: "TestIndexHtmlCarousel stub class with 6 failing test methods"
      contains: "class TestIndexHtmlCarousel"
  key_links:
    - from: "tests/test_static_export_service.py::TestIndexHtmlCarousel"
      to: "StaticExportService._build_index_html()"
      via: "svc.export(tmp_path) then (tmp_path / 'index.html').read_text()"
      pattern: "svc\\.export\\(tmp_path\\)"
---

<objective>
Create failing test stubs for all carousel assertions in `tests/test_static_export_service.py`.

Purpose: Nyquist Wave 0 compliance — every automated test must exist and FAIL before implementation begins. This plan creates the `TestIndexHtmlCarousel` class with 6 methods that assert carousel strings are present in the generated `index.html`. Since no implementation exists yet, every assertion will fail. Wave 1 makes them green.

Output: `TestIndexHtmlCarousel` class appended to `tests/test_static_export_service.py` after the existing `TestIndexHtml` class (after line 246). Tests run in < 2s and exit non-zero.
</objective>

<execution_context>
@/Users/ffaber/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ffaber/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-viewer-presentation-modes/16-RESEARCH.md
@.planning/phases/16-viewer-presentation-modes/16-VALIDATION.md
@.planning/phases/16-viewer-presentation-modes/16-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TestIndexHtmlCarousel stub class with 6 failing assertions</name>
  <files>tests/test_static_export_service.py</files>

  <read_first>
    - tests/test_static_export_service.py (full file — executor must read to find line 246 where TestIndexHtml ends and to match the exact fixture signature `def test_...(self, temp_db_v6, tmp_path):`)
    - tests/conftest.py (lines 430–445 — verify `temp_db_v6` fixture exists and yields a sqlite3.Connection)
  </read_first>

  <behavior>
    Each test method must call `svc.export(tmp_path)`, read `(tmp_path / "index.html").read_text()`, then assert a string that does NOT yet exist in the HTML. Before Wave 1 implementation, all 6 assertions fail because the HTML string in `_build_index_html()` does not yet contain any of the carousel identifiers.

    Required test methods and their assertions:
    1. `test_mode_switcher_button_class_present` — asserts `"mode-btn" in html`
    2. `test_localstorage_key_present` — asserts `"xbm_mode" in html`
    3. `test_carousel_render_function_present` — asserts `"renderCarousel" in html` AND `"carouselIndex" in html`
    4. `test_keyboard_nav_listener_present` — asserts `"ArrowRight" in html` AND `"ArrowLeft" in html` AND `"Escape" in html`
    5. `test_carousel_nav_dom_ids_present` — asserts `"carousel-nav" in html` AND `"carousel-prev" in html` AND `"carousel-next" in html` AND `"carousel-counter" in html`
    6. `test_oembed_reinit_called_in_carousel` — asserts `"twttr.widgets.load" in html`

    Note: `twttr.widgets.load` already exists in the HTML from Phase 15's stream oEmbed path (line 854 in static_export.py). The assertion on test_oembed_reinit_called_in_carousel will therefore PASS even before Wave 1. This is acceptable — the test verifies the string is present, which it already is. The other 5 tests will fail.
  </behavior>

  <action>
  Append `TestIndexHtmlCarousel` class to `tests/test_static_export_service.py` immediately after the `TestIndexHtml` class ends (after line 246, before the `class TestNetlifyToml:` that currently starts at line 248).

  The exact class to append (insert between line 246 and line 248):

  ```python
  class TestIndexHtmlCarousel:
      """Tests for carousel mode additions to index.html. VIEWER-01 through VIEWER-05."""

      def test_mode_switcher_button_class_present(self, temp_db_v6, tmp_path):
          """VIEWER-01: mode switcher button class present in generated HTML."""
          from src.services.static_export import StaticExportService
          svc = StaticExportService(temp_db_v6)
          svc.export(tmp_path)
          html = (tmp_path / "index.html").read_text()
          assert "mode-btn" in html

      def test_localstorage_key_present(self, temp_db_v6, tmp_path):
          """VIEWER-02: localStorage key xbm_mode present in generated HTML."""
          from src.services.static_export import StaticExportService
          svc = StaticExportService(temp_db_v6)
          svc.export(tmp_path)
          html = (tmp_path / "index.html").read_text()
          assert "xbm_mode" in html

      def test_carousel_render_function_present(self, temp_db_v6, tmp_path):
          """VIEWER-03: renderCarousel function and carouselIndex variable present."""
          from src.services.static_export import StaticExportService
          svc = StaticExportService(temp_db_v6)
          svc.export(tmp_path)
          html = (tmp_path / "index.html").read_text()
          assert "renderCarousel" in html
          assert "carouselIndex" in html

      def test_keyboard_nav_listener_present(self, temp_db_v6, tmp_path):
          """VIEWER-04: keyboard nav listener with ArrowRight/ArrowLeft/Escape present."""
          from src.services.static_export import StaticExportService
          svc = StaticExportService(temp_db_v6)
          svc.export(tmp_path)
          html = (tmp_path / "index.html").read_text()
          assert "ArrowRight" in html
          assert "ArrowLeft" in html
          assert "Escape" in html

      def test_carousel_nav_dom_ids_present(self, temp_db_v6, tmp_path):
          """VIEWER-04/05: carousel-nav, carousel-prev, carousel-next, carousel-counter present."""
          from src.services.static_export import StaticExportService
          svc = StaticExportService(temp_db_v6)
          svc.export(tmp_path)
          html = (tmp_path / "index.html").read_text()
          assert "carousel-nav" in html
          assert "carousel-prev" in html
          assert "carousel-next" in html
          assert "carousel-counter" in html

      def test_oembed_reinit_called_in_carousel(self, temp_db_v6, tmp_path):
          """VIEWER-05: twttr.widgets.load present for oEmbed re-init in carousel."""
          from src.services.static_export import StaticExportService
          svc = StaticExportService(temp_db_v6)
          svc.export(tmp_path)
          html = (tmp_path / "index.html").read_text()
          assert "twttr.widgets.load" in html
  ```

  Insert this class between line 246 (end of `TestIndexHtml`) and line 248 (start of `TestNetlifyToml`). Do not remove any existing content. Add a blank line before and after the new class for standard Python formatting.
  </action>

  <verify>
    <automated>cd /Users/ffaber/claude-projects/x-bookmarked-posts && venv/bin/python -m pytest tests/test_static_export_service.py::TestIndexHtmlCarousel -x -q 2>&1 | tail -15</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "class TestIndexHtmlCarousel" tests/test_static_export_service.py` returns a line number between 246 and 260
    - `grep -c "def test_" tests/test_static_export_service.py` returns a count 6 higher than before (was 16, now 22)
    - `venv/bin/python -m pytest tests/test_static_export_service.py::TestIndexHtmlCarousel -x -q` exits with failures — at minimum `test_mode_switcher_button_class_present`, `test_localstorage_key_present`, `test_carousel_render_function_present`, `test_keyboard_nav_listener_present`, `test_carousel_nav_dom_ids_present` fail (5 of 6 stubs fail, 1 may pass because `twttr.widgets.load` already present from Phase 15)
    - `venv/bin/python -m pytest tests/test_static_export_service.py::TestIndexHtml -x -q` passes all 8 methods (no regressions)
    - `venv/bin/python -m pytest tests/test_static_export_service.py -x -q 2>&1 | grep "passed"` shows 32 or 33 passed (1 stub may pass due to twttr.widgets.load already present)
  </acceptance_criteria>

  <done>
    `TestIndexHtmlCarousel` class is present in `tests/test_static_export_service.py` with 6 methods. Running `pytest tests/test_static_export_service.py::TestIndexHtmlCarousel` exits non-zero with at least 5 failures. Existing `TestIndexHtml` tests still pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test file | No new trust boundaries — test file is developer-controlled, no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-00-01 | Tampering | tests/test_static_export_service.py | accept | Test file is version-controlled; no external input path |
</threat_model>

<verification>
After Task 1:
- `venv/bin/python -m pytest tests/test_static_export_service.py::TestIndexHtmlCarousel -x -q` exits non-zero (stubs fail)
- `venv/bin/python -m pytest tests/test_static_export_service.py::TestIndexHtml -x -q` exits 0 (no regressions)
- `grep "class TestIndexHtmlCarousel" tests/test_static_export_service.py` finds the class
</verification>

<success_criteria>
- `TestIndexHtmlCarousel` class exists in `tests/test_static_export_service.py`
- All 6 test methods are present and runnable
- At least 5 of 6 stubs fail (carousel strings not yet in HTML)
- Zero regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/16-viewer-presentation-modes/16-00-SUMMARY.md`
</output>
