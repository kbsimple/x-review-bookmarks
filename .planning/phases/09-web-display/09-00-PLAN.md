---
phase: 09-web-display
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: [tests/test_web_browse.py]
autonomous: true
requirements: [WEB-07, WEB-08, WEB-09, WEB-10]
must_haves:
  truths:
    - "Test infrastructure exists for embedded post rendering"
    - "Mock data includes quote posts, retweet posts, and unavailable posts"
    - "Tests verify quote cards show user commentary above nested content"
    - "Tests verify retweets show attribution and original content"
    - "Tests verify unavailable posts show placeholder with author if known"
  artifacts:
    - path: "tests/test_web_browse.py"
      provides: "Test class for embedded post rendering"
      contains: "class TestEmbeddedPosts"
  key_links:
    - from: "tests/test_web_browse.py"
      to: "src/web/routes/browse.py"
      via: "pytest test execution"
---

<objective>
Create test infrastructure for embedded post rendering (WEB-07, WEB-08, WEB-09, WEB-10). Add mock data for quote tweets, retweets, and unavailable embedded posts to support testing of conditional post card rendering.

Purpose: Tests must exist BEFORE implementation to drive TDD and verify requirements.
Output: Test class with mock data covering all embedded post types.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-web-display/09-CONTEXT.md
@.planning/phases/09-web-display/09-RESEARCH.md
@.planning/phases/09-web-display/09-VALIDATION.md
@.planning/phases/09-web-display/09-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TestEmbeddedPosts test class with mock data</name>
  <files>tests/test_web_browse.py</files>
  <read_first>
    tests/test_web_browse.py
    .planning/phases/09-web-display/09-CONTEXT.md
    .planning/phases/09-web-display/09-UI-SPEC.md
  </read_first>
  <behavior>
    - Test: quote tweet shows user text + nested card with attribution (WEB-07)
    - Test: retweet shows attribution header + original content (WEB-08)
    - Test: unavailable shows placeholder with author if known (WEB-10)
    - Test: embedded media displays in adaptive grid (WEB-09)
    - Test: XSS prevention in embedded post content (security)
  </behavior>
  <action>
Add a new test class `TestEmbeddedPosts` to tests/test_web_browse.py with the following:

1. Create MOCK_EMBEDDED_POSTS list with sample embedded post data:
   - One quote tweet (post_type="quote") with embedded_post_id referencing the quoted post
   - One retweet (post_type="retweet") with embedded_post_id referencing the original
   - One unavailable embedded post (available=False)
   - One embedded post with media_urls (for media grid testing)

2. Create MOCK_POSTS_FOR_EMBEDDED list containing posts that reference embedded posts:
   - Quote post: post_type="quote", embedded_post_id set, text is the quoter's commentary
   - Retweet post: post_type="retweet", embedded_post_id set, text is empty or "RT @user"
   - Original post: post_type="original", embedded_post_id=None

3. Add fixture `mock_db_with_embedded_posts` that:
   - Creates temporary database with schema V6 (including embedded_posts table)
   - Inserts MOCK_POSTS_FOR_EMBEDDED into posts table
   - Inserts MOCK_EMBEDDED_POSTS into embedded_posts table
   - Yields the db_path

4. Add fixture `test_client_with_embedded` that:
   - Patches browse.py Path to use mock_db_with_embedded_posts
   - Creates TestClient for the app
   - Yields the client

5. Write test stubs (will fail until implementation):
   - test_quote_tweet_display: Verify quote tweet shows user commentary and nested card
   - test_retweet_display: Verify retweet shows attribution and original content
   - test_unavailable_placeholder: Verify unavailable post shows placeholder
   - test_embedded_media_display: Verify embedded media in adaptive grid
   - test_no_xss_in_embedded: Verify embedded post content is escaped

The test stubs should use placeholder assertions that will pass once the actual implementation exists. Each test should verify the specific elements required by CONTEXT.md decisions D-01 through D-06.
  </action>
  <verify>
    <automated>pytest tests/test_web_browse.py::TestEmbeddedPosts -v --collect-only | grep -E "test_quote_tweet_display|test_retweet_display|test_unavailable_placeholder|test_embedded_media_display|test_no_xss_in_embedded"</automated>
  </verify>
  <done>
    - test_web_browse.py contains class TestEmbeddedPosts
    - MOCK_EMBEDDED_POSTS and MOCK_POSTS_FOR_EMBEDDED lists defined
    - mock_db_with_embedded_posts fixture creates posts and embedded_posts tables
    - 5 test method stubs exist in TestEmbeddedPosts class
    - Tests are discoverable by pytest
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test data → Application | Mock data simulates untrusted user content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-00 | Tampering | Test data | accept | Test fixtures use safe mock data |
</threat_model>

<verification>
Test class compiles and is discoverable by pytest. Mock data includes all required post types (quote, retweet, unavailable, media). Fixture creates both posts and embedded_posts tables.
</verification>

<success_criteria>
- TestEmbeddedPosts class exists in test_web_browse.py
- Mock data covers quote tweets, retweets, unavailable posts, media posts
- Fixture creates embedded_posts table with test data
- All 5 test stubs are discoverable by pytest
</success_criteria>

<output>
After completion, create `.planning/phases/09-web-display/09-00-SUMMARY.md`
</output>